#include <stdio.h>
int main()
{
    int n, h, sum = 0;
    printf("Enter a value\n ");
    scanf("%d",&n);
    for(h=1; h <= n; ++h)
    {
        sum += i;   
    }
    printf("Sum = %d",sum);
    return 0;
}
